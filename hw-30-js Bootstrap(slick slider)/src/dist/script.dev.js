"use strict";

$(document).ready(function () {
  $('.slider').slick({
    arrows: true,
    centerMode: true,
    slidesToShow: 4,
    autoplay: true,
    speed: 1000,
    autoplaySpeed: 1500
  });
});